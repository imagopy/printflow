{
  "name": "printflow",
  "version": "1.0.0",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "cd backend && npm ci --only=production && npx prisma generate && npm run build",
    "start": "cd backend && npm start",
    "postinstall": "cd backend && npm ci && npx prisma generate"
  }
}
